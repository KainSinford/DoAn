services:
  - name: order-service
    url: http://host.docker.internal:5000
    routes:
      - name: order-route
        paths:
          - /orders
    plugins:        
      - name: key-auth
        config:
          key_names: [apikey]
      - name: rate-limiting
        config:
          second: 5
          policy: local